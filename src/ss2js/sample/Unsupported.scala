package ss2js.sample

import ss2js.global._

/*
class Unsupported {
    def this(a: Int) = this  // multiple constructors

    case class Abc()  // case class
    trait Trait2 { val a = 1 }  // concrete trait

    lazy val a = 1  // lazy
    val c = if(true) { 1; 2 }  // block if returning value
    1 match { case _ => 1 }  // non Unit match

    var b = 0
    try b = 1
    catch { case e if b == 3 => "";  // guard
    	case _  => "" }  // multiple cases
    finally b = 2

    def b(a: Int) {}  // function and variable with same name
    def apply() {}  // apply
    def abc {}  // no param list
    def abc3(a: Int*) {}  // TODO: varargs
    def check[T](func: => T) = func  // function with missing arg(s)

}
*/
